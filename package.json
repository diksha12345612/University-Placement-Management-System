{
  "name": "university-placement-system",
  "version": "1.0.0",
  "description": "Cross-platform project management for University Placement Portal",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix server & npm run dev --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build:all": "npm run build --prefix client",
    "seed": "npm run seed --prefix server",
    "lint:all": "npm run lint --prefix client && npm run lint --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
